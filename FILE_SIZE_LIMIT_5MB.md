# 🔧 图纸申报文件大小限制（5MB）

## 功能说明

限制图纸申报上传文件的大小为 **5MB 以内**。

## 实现方案

### 1. 定义文件大小常量

**文件**：`src/components/DrawingSubmission.tsx`

**位置**：第 60-61 行

**代码**：
```typescript
// 文件大小限制：5MB
const MAX_FILE_SIZE = 5 * 1024 * 1024; // 5MB in bytes
```

### 2. 在上传前检查文件大小

**文件**：`src/components/DrawingSubmission.tsx`

**位置**：第 150-154 行（uploadFile 函数）

**代码**：
```typescript
// 检查文件大小
if (file.size > MAX_FILE_SIZE) {
  alert(`文件大小超过限制！最大允许 5MB，当前文件大小 ${(file.size / 1024 / 1024).toFixed(2)}MB`);
  return null;
}
```

## 修改文件

### `src/components/DrawingSubmission.tsx`

**修改 1**：添加常量（第 60-61 行）
```typescript
// 文件大小限制：5MB
const MAX_FILE_SIZE = 5 * 1024 * 1024; // 5MB in bytes
```

**修改 2**：在 uploadFile 函数中添加检查（第 150-154 行）
```typescript
const uploadFile = async (file: File, docKey: string) => {
  if (isPreviewMode) {
    alert('预览模式下无法上传文件');
    return null;
  }

  // 检查文件大小
  if (file.size > MAX_FILE_SIZE) {
    alert(`文件大小超过限制！最大允许 5MB，当前文件大小 ${(file.size / 1024 / 1024).toFixed(2)}MB`);
    return null;
  }

  // 继续上传...
};
```

## 工作流程

### 用户上传文件时

```
用户选择文件
    ↓
检查文件大小
    ↓
文件 ≤ 5MB？
    ├─ 是 → 继续上传
    └─ 否 → 显示错误提示，停止上传
```

### 错误提示示例

**文件过大时**：
```
文件大小超过限制！最大允许 5MB，当前文件大小 8.50MB
```

**文件正常时**：
- 无提示
- 继续上传

## 代码质量

- ✅ 无 TypeScript 错误
- ✅ 无 ESLint 警告
- ✅ 逻辑清晰
- ✅ 用户友好的错误提示

## 功能特点

- ✅ **简单高效**：在上传前检查文件大小
- ✅ **用户友好**：显示当前文件大小
- ✅ **防止浪费**：避免上传超大文件
- ✅ **节省带宽**：减少不必要的网络传输

## 测试验证

### 测试 1：上传小于 5MB 的文件

**步骤**：
1. 选择一个小于 5MB 的文件
2. 点击上传

**预期结果**：
- ✅ 文件上传成功
- ✅ 无错误提示

### 测试 2：上传大于 5MB 的文件

**步骤**：
1. 选择一个大于 5MB 的文件
2. 点击上传

**预期结果**：
- ❌ 显示错误提示：`文件大小超过限制！最大允许 5MB，当前文件大小 X.XXMB`
- ❌ 文件不上传

### 测试 3：上传恰好 5MB 的文件

**步骤**：
1. 选择一个恰好 5MB 的文件
2. 点击上传

**预期结果**：
- ✅ 文件上传成功
- ✅ 无错误提示

## 限制说明

### 为什么限制为 5MB？

**原因**：
1. **网络传输**：减少上传时间
2. **存储空间**：节省服务器存储
3. **用户体验**：避免长时间等待
4. **安全性**：防止恶意大文件上传

### 5MB 的合理性

**对于图纸文件**：
- ✅ 高分辨率图片：通常 2-4MB
- ✅ PDF 文档：通常 1-3MB
- ✅ 压缩文件：通常 1-5MB
- ✅ 足够满足大多数需求

## 修改影响

### 对现有功能的影响

- ✅ 不影响已上传的文件
- ✅ 不影响其他功能
- ✅ 只在上传时进行检查

### 对用户的影响

- ✅ 提升用户体验（快速反馈）
- ✅ 防止上传失败
- ✅ 清晰的错误提示

## 总结

这个功能确保了：
1. ✅ 文件大小不超过 5MB
2. ✅ 用户得到清晰的错误提示
3. ✅ 节省网络带宽和存储空间
4. ✅ 提升系统稳定性

**关键点**：在上传前检查文件大小，超过 5MB 则拒绝上传并显示错误提示。
