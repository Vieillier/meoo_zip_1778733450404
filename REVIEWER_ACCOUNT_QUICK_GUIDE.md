# 🚀 审图员账号管理 - 快速使用指南

## 📋 功能概述

管理员可以在用户管理界面新增、编辑、删除审图员账号。

## 🎯 快速开始

### 1. 新增审图员（3 步）

```
1. 管理员登录 → 用户管理 → 审图员列表
2. 点击"新增审图员"按钮
3. 填写信息并确认
   - 账号：reviewer1
   - 密码：123（自动变为 123_secure）
   - 显示名称：审图员1
```

### 2. 审图员登录

```
账号：reviewer1
密码：123
```

### 3. 编辑审图员

```
1. 审图员列表 → 点击"编辑"
2. 修改信息
3. 点击"确认"
```

### 4. 删除审图员

```
1. 审图员列表 → 点击"删除"
2. 确认删除
```

## 🔑 密码规则

| 输入密码 | 实际密码 | 说明 |
|---------|---------|------|
| `123` | `123_secure` | < 6 位自动添加后缀 |
| `admin` | `admin_secure` | < 6 位自动添加后缀 |
| `admin123` | `admin123` | ≥ 6 位保持原密码 |
| `reviewer01` | `reviewer01` | ≥ 6 位保持原密码 |

## 📊 界面布局

```
用户管理
├─ [展商列表] [审图员列表]  ← 标签页切换
│
├─ 展商列表（原有功能）
│  ├─ 新增用户
│  ├─ 导入展商表格
│  └─ 展商列表表格
│
└─ 审图员列表（新增功能）
   ├─ 新增审图员
   └─ 审图员列表表格
      ├─ 账号
      ├─ 显示名称
      └─ 操作（编辑、删除）
```

## ✅ 权限说明

### 管理员
- ✅ 可以查看审图员列表
- ✅ 可以新增审图员
- ✅ 可以编辑审图员
- ✅ 可以删除审图员

### 审图员
- ❌ 不能查看审图员列表
- ❌ 不能看到其他审图员
- ✅ 多个审图员共享同一个路由
- ✅ 可以同时操作不同板块

## 🎨 界面预览

### 审图员列表

```
┌─────────────────────────────────────────────┐
│ 用户管理                                     │
├─────────────────────────────────────────────┤
│ [展商列表] [审图员列表] ← 标签页             │
├─────────────────────────────────────────────┤
│ [新增审图员]                                 │
├─────────────────────────────────────────────┤
│ 账号        │ 显示名称  │ 操作              │
├─────────────┼──────────┼───────────────────┤
│ reviewer1   │ 审图员1   │ [编辑] [删除]     │
│ reviewer2   │ 审图员2   │ [编辑] [删除]     │
└─────────────────────────────────────────────┘
```

### 新增审图员弹窗

```
┌─────────────────────────────────┐
│ 新增审图员                       │
├─────────────────────────────────┤
│ 账号：[________________]        │
│ 密码：[________________]        │
│      （小于6位自动添加_secure）  │
│ 显示名称：[________________]    │
├─────────────────────────────────┤
│ [确认]  [取消]                  │
└─────────────────────────────────┘
```

## 📝 常见问题

### Q1：审图员密码规则是什么？
**A**：与展商账号相同，小于 6 位自动添加 `_secure` 后缀。

### Q2：审图员可以看到其他审图员吗？
**A**：不可以，只有管理员可以看到审图员列表。

### Q3：多个审图员可以同时登录吗？
**A**：可以，多个审图员共享同一个路由，可以同时操作。

### Q4：审图员列表在哪里？
**A**：管理员路由 → 用户管理 → 审图员列表标签。

### Q5：如何修改审图员密码？
**A**：在审图员列表中点击"编辑"，输入新密码即可。

## 🔍 测试步骤

### 完整测试流程（5 分钟）

```
1. 新增审图员
   - 管理员登录
   - 用户管理 → 审图员列表
   - 新增审图员：reviewer1 / 123 / 审图员1
   - ✅ 创建成功

2. 审图员登录
   - 退出管理员
   - 使用 reviewer1 / 123 登录
   - ✅ 登录成功，进入审图员界面

3. 验证权限
   - 审图员界面不显示审图员列表
   - ✅ 权限正确

4. 编辑审图员
   - 管理员登录
   - 编辑 reviewer1 的显示名称
   - ✅ 更新成功

5. 删除审图员
   - 删除 reviewer1
   - ✅ 删除成功
```

## 📚 相关文档

- `REVIEWER_ACCOUNT_MANAGEMENT.md` - 详细功能说明

## 🎉 功能完成

✅ **审图员账号管理功能已实现**

现在管理员可以：
- ✅ 新增审图员账号
- ✅ 编辑审图员信息
- ✅ 删除审图员账号
- ✅ 查看审图员列表

**下一步**：
1. 启动开发服务器：`npm run dev`
2. 管理员登录测试
3. 新增审图员测试
4. 审图员登录测试
