# 图纸审核流程 - 可再次驳回功能说明

## 功能概述

在审图员图纸审核全部通过后，增加"可再次驳回"按钮，允许审图员重新驳回此次审核，让展商可以再次进入申报模式进行整改。

## 数据流转逻辑

### 1. 初始状态（展商首次提交）
- **展商端**：上传图纸 → 点击"确认提交所有图纸"
- **数据库**：`is_submitted = true`, 所有图纸状态 = `pending`
- **展商界面**：显示"等待审核"标签，不可操作

### 2. 审图员审核阶段
- **审图员端**：打开图纸审核弹窗，对每张图纸选择"通过"或"不通过"
- **两种结果**：
  - **全部通过**：`is_submitted = false`, `last_reviewed_at` 记录时间
    - 展商界面显示"所有图纸已通过审核"，不可操作
    - 审图员界面显示"审核已完成" + **"可再次驳回"按钮**
  - **有驳回**：`is_submitted = true`, 各图纸状态为 `approved/rejected`, 记录审核意见
    - 展商界面显示"等待审核"，可点击"开启修改模式"

### 3. 可再次驳回流程（新增）
- **审图员操作**：点击"可再次驳回"按钮
- **确认对话**：弹出确认框"确认要驳回此次审核吗？展商将需要重新提交整改申报。"
- **数据变更**：
  - 所有图纸状态重置为 `pending`
  - 清空所有审核意见（`comment = ''`）
  - `is_submitted = true`（展商需要重新提交）
  - `last_reviewed_at = null`（清空审核时间）
- **展商界面**：恢复到"等待审核"状态，可再次进入修改模式

### 4. 展商整改流程
- **展商操作**：点击"开启修改模式" → 修改图纸 → 点击"提交整改申报"
- **数据变更**：
  - `review_round` 递增
  - 所有图纸状态重置为 `pending`
  - `is_submitted = true`
  - 新的整改轮次记录到 `drawing_history`

## 关键代码变更

### DrawingReview.tsx

**新增函数**：`handleRejectAgain()`
```typescript
const handleRejectAgain = async () => {
  // 1. 确认对话
  // 2. 重置所有图纸状态为 pending
  // 3. 清空审核意见
  // 4. 设置 is_submitted = true
  // 5. 清空 last_reviewed_at
}
```

**按钮逻辑**：
- 审核未完成：显示"提交审核意见"或"审核通过"按钮
- 审核已完成且全部通过：显示"审核已完成" + **"可再次驳回"按钮**
- 审核已完成但有驳回：只显示"审核已完成"（不显示驳回按钮）

## 数据库字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `is_submitted` | boolean | 是否已提交（true=待审核，false=审核通过） |
| `last_reviewed_at` | timestamp | 最后审核时间（null=未审核或已驳回） |
| `review_round` | integer | 审核轮次（0=初次，1+=整改轮次） |
| `*_status` | enum | 图纸状态（pending/approved/rejected） |
| `*_comment` | text | 审核意见 |

## 用户体验流程

```
展商提交 → 审图员审核 → 全部通过
                    ↓
            审图员可再次驳回
                    ↓
            展商重新整改提交
                    ↓
            审图员再次审核
```
