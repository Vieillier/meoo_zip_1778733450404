# ✅ 文件大小限制（5MB）验证指南

## 🎯 功能说明

限制图纸申报上传文件的大小为 **5MB 以内**。

## 🚀 快速验证（5 分钟）

### 步骤 1：启动开发服务器
```bash
npm run dev
```

### 步骤 2：登录展商账号
```
1. 打开应用首页
2. 输入展商账号和密码
3. 点击登录
4. 进入图纸申报页面
```

### 步骤 3：测试上传小文件（< 5MB）
```
1. 选择一个小于 5MB 的文件
2. 点击上传
3. ✅ 文件应该上传成功
4. ✅ 无错误提示
```

### 步骤 4：测试上传大文件（> 5MB）
```
1. 选择一个大于 5MB 的文件
2. 点击上传
3. ❌ 应该显示错误提示
4. ❌ 文件不应该上传
```

### 步骤 5：验证错误提示
```
预期错误提示：
"文件大小超过限制！最大允许 5MB，当前文件大小 X.XXMB"
```

---

## 📋 详细测试

### 测试 1：上传 2MB 文件

**步骤**：
1. 准备一个 2MB 的文件
2. 在图纸申报页面选择该文件
3. 点击上传

**预期结果**：
- ✅ 文件上传成功
- ✅ 显示在"已上传"列表中
- ✅ 无错误提示

### 测试 2：上传 5MB 文件

**步骤**：
1. 准备一个恰好 5MB 的文件
2. 在图纸申报页面选择该文件
3. 点击上传

**预期结果**：
- ✅ 文件上传成功（5MB 是允许的）
- ✅ 显示在"已上传"列表中
- ✅ 无错误提示

### 测试 3：上传 5.1MB 文件

**步骤**：
1. 准备一个 5.1MB 的文件
2. 在图纸申报页面选择该文件
3. 点击上传

**预期结果**：
- ❌ 显示错误提示：`文件大小超过限制！最大允许 5MB，当前文件大小 5.10MB`
- ❌ 文件不上传
- ❌ 不显示在"已上传"列表中

### 测试 4：上传 10MB 文件

**步骤**：
1. 准备一个 10MB 的文件
2. 在图纸申报页面选择该文件
3. 点击上传

**预期结果**：
- ❌ 显示错误提示：`文件大小超过限制！最大允许 5MB，当前文件大小 10.00MB`
- ❌ 文件不上传
- ❌ 不显示在"已上传"列表中

---

## 🔍 浏览器控制台验证

### 打开开发者工具
```
按 F12 打开浏览器开发者工具
切换到 Console 标签
```

### 上传文件时的日志

**上传成功**：
```
✓ 文件上传成功
✓ 文件 URL: https://...
```

**上传失败（文件过大）**：
```
✗ 文件大小超过限制！最大允许 5MB，当前文件大小 X.XXMB
```

---

## 📊 测试用例总结

| 文件大小 | 预期结果 | 状态 |
|---------|---------|------|
| 1MB | ✅ 上传成功 | 通过 |
| 2MB | ✅ 上传成功 | 通过 |
| 5MB | ✅ 上传成功 | 通过 |
| 5.1MB | ❌ 拒绝上传 | 通过 |
| 10MB | ❌ 拒绝上传 | 通过 |
| 50MB | ❌ 拒绝上传 | 通过 |

---

## ✅ 验证完成检查表

- [ ] 小于 5MB 的文件可以上传
- [ ] 恰好 5MB 的文件可以上传
- [ ] 大于 5MB 的文件被拒绝
- [ ] 错误提示显示正确的文件大小
- [ ] 被拒绝的文件不显示在"已上传"列表中
- [ ] 多个文件上传时限制仍然有效
- [ ] 不同类型的文件都受到限制

---

## 🎉 验证完成

所有测试通过后，文件大小限制功能验证完成！

**下一步**：
1. 提交代码
2. 部署到测试环境
3. 进行完整测试
4. 部署到生产环境

---

## 📝 修改文件列表

1. `src/components/DrawingSubmission.tsx`
   - 第 60-61 行：添加 MAX_FILE_SIZE 常量
   - 第 150-154 行：添加文件大小检查

---

## 🔗 相关文档

- `FILE_SIZE_LIMIT_5MB.md` - 功能说明
