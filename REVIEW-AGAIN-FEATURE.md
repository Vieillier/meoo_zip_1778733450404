# "再次审查"功能 - 实现说明

## 🎯 功能需求

在图纸审核"审核已完成"状态下，增加"再次审查"按钮，用于解锁页面状态，使得审图员可以重新审图。

---

## ✅ 实现方案

### 1. 核心逻辑

**新增函数**：`handleReviewAgain()` 在 `DrawingReview.tsx`

**功能**：
- 确认对话框防止误操作
- 清空 `last_reviewed_at` 字段（解锁审核状态）
- 重新加载数据，页面恢复到可编辑状态
- **不修改**审核结果和意见（保留现有的通过/驳回状态）

**与"可再次驳回"的区别**：
- **再次审查**：仅解锁状态，保留所有审核结果，允许修改
- **可再次驳回**：重置所有状态为 pending，清空审核意见，展商需重新提交

---

## 🎨 UI 设计

### 按钮布局

#### 审核未完成状态
```
┌──────────────────────────────────────────────────────┐
│  [提交审核意见]  [AI 初审]  [关闭]                     │
└──────────────────────────────────────────────────────┘
```

#### 审核已完成 + 有驳回
```
┌──────────────────────────────────────────────────────┐
│  [审核已完成]  [再次审查]  [关闭]                      │
└──────────────────────────────────────────────────────┘
```

#### 审核已完成 + 全部通过
```
┌──────────────────────────────────────────────────────┐
│  [审核已完成]  [再次审查]  [可再次驳回]  [关闭]        │
└──────────────────────────────────────────────────────┘
```

### 按钮样式

**"再次审查"按钮**：
- 颜色：蓝色（`bg-blue-600`）
- 图标：刷新图标（`fa-redo`）
- 文字：`再次审查`
- Hover：深蓝色（`hover:bg-blue-700`）
- Tooltip：`解锁审核状态，重新审查`

---

## 📋 使用场景

### 场景 1：发现审核错误，需要修改

**操作流程**：
1. 审图员完成审核，状态显示"审核已完成"
2. 发现某个图纸审核结果有误
3. 点击"再次审查"按钮
4. 确认对话框 → 点击"确定"
5. 页面解锁，所有图纸的"通过"/"不通过"按钮重新可点击
6. 修改审核结果和意见
7. 重新点击"提交审核意见"

### 场景 2：展商补充了新图纸

**操作流程**：
1. 审图员之前已完成审核
2. 展商上传了新的补充图纸
3. 审图员点击"再次审查"解锁状态
4. 查看新图纸并审核
5. 重新提交审核意见

### 场景 3：需要重新使用 AI 初审

**操作流程**：
1. 审图员完成审核后，想再次使用 AI 验证
2. 点击"再次审查"解锁状态
3. 点击"AI 初审"按钮
4. 对比 AI 建议和自己的审核结果
5. 确认无误后重新提交

---

## 🔧 技术实现

### 核心代码

```typescript
const handleReviewAgain = async () => {
  // 1. 确认对话框
  if (!confirm('确认要重新审查吗？这将解锁审核状态，允许您修改审核结果。')) {
    return;
  }

  setSubmitting(true);
  try {
    // 2. 查询现有记录
    const { data: existing } = await supabase
      .from('drawing_documents')
      .select('id')
      .eq('booth_number', boothNumber)
      .maybeSingle();

    if (!existing) {
      alert('未找到审核记录');
      return;
    }

    // 3. 清空 last_reviewed_at，解锁审核状态
    await supabase
      .from('drawing_documents')
      .update({ last_reviewed_at: null })
      .eq('id', existing.id);

    // 4. 重新加载数据
    await fetchDrawings();
    alert('已解锁审核状态，您可以重新审查');
  } catch (error) {
    alert('解锁失败: ' + (error as Error).message);
  }
  setSubmitting(false);
};
```

### 数据变更

**修改前**：
```json
{
  "last_reviewed_at": "2026-05-21T14:30:00Z",
  "effect_drawing_status": "approved",
  "effect_drawing_comment": "效果图清晰，符合要求",
  "plan_drawing_status": "rejected",
  "plan_drawing_comment": "平面图尺寸标注不清晰"
}
```

**点击"再次审查"后**：
```json
{
  "last_reviewed_at": null,  // ← 仅清空这个字段
  "effect_drawing_status": "approved",  // ← 保留
  "effect_drawing_comment": "效果图清晰，符合要求",  // ← 保留
  "plan_drawing_status": "rejected",  // ← 保留
  "plan_drawing_comment": "平面图尺寸标注不清晰"  // ← 保留
}
```

**页面状态变化**：
- `isReviewed` 从 `true` 变为 `false`
- 所有图纸的"通过"/"不通过"按钮重新可点击
- 审核意见输入框重新可编辑
- 底部按钮从"审核已完成"变为"提交审核意见"

---

## 🆚 功能对比

| 功能 | 再次审查 | 可再次驳回 |
|------|---------|-----------|
| **按钮颜色** | 蓝色 | 橙色 |
| **显示条件** | 审核已完成 | 审核已完成 + 全部通过 |
| **操作** | 解锁状态 | 重置状态 + 展商重新提交 |
| **审核结果** | 保留 | 清空（重置为 pending） |
| **审核意见** | 保留 | 清空 |
| **展商状态** | 不变 | 需要重新提交 |
| **使用场景** | 审图员修改自己的审核 | 审图员驳回已通过的审核 |

---

## ⚠️ 注意事项

1. **保留审核结果**
   - "再次审查"不会清空已有的审核结果和意见
   - 审图员可以在原有基础上修改

2. **不影响展商**
   - 展商端不会收到任何通知
   - 展商的提交状态不变

3. **可重复使用**
   - 审图员可以多次点击"再次审查"
   - 每次都会解锁状态

4. **与 AI 初审配合**
   - 解锁后可以使用"AI 初审"功能
   - AI 建议会覆盖现有的审核意见（如果选择应用）

---

## 🎉 完成效果

审图员现在有三种操作选项：

1. **再次审查**（蓝色）
   - 用途：修改自己的审核结果
   - 影响：仅解锁页面，不影响展商

2. **可再次驳回**（橙色，仅全部通过时显示）
   - 用途：驳回已通过的审核
   - 影响：展商需要重新提交

3. **AI 初审**（紫色，仅未完成时显示）
   - 用途：使用 AI 辅助审核
   - 影响：自动填写审核意见

完美的审图工作流！✅
