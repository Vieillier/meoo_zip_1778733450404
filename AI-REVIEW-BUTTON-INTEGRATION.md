# AI 初审按钮 - 前端集成完成

## 🎉 功能实现

已在**图纸审核弹窗**中成功添加"AI 初审"按钮！

---

## 📍 按钮位置

**路径**：特装审图页面 → 点击"图纸审核" → 弹窗底部

**布局**：
```
┌─────────────────────────────────────────────────────┐
│  [提交审核意见]  [AI 初审]  [关闭]                    │
└─────────────────────────────────────────────────────┘
```

- **左侧**：提交审核意见 / 审核通过（蓝色/绿色）
- **中间**：AI 初审（紫色，带机器人图标）
- **右侧**：关闭（灰色）

---

## ⚙️ 功能逻辑

### 1. 点击"AI 初审"按钮

**触发条件**：
- ✅ 审核未完成（`!isReviewed`）
- ✅ 已获取到 booth_id
- ✅ 不在提交中

**执行流程**：
1. 弹出确认对话框
2. 显示"AI 分析中..."加载状态
3. 调用 AI 初审 Edge Function
4. 解析 AI 响应

### 2. AI 建议"驳回"时

**自动操作**：
- 找到第一个有图纸的项目
- 自动点击"不通过"按钮
- 自动填写审核意见：
  ```
  【AI 初审意见】
  [AI 的详细理由]
  
  请审图员确认或修改此意见。
  ```

**提示信息**：
```
AI 初审完成！

建议：驳回

已自动将"[图纸类型]"标记为不通过，并填写了审核意见。
请审图员确认或修改。
```

### 3. AI 建议"通过"时

**提示信息**：
```
AI 初审完成！

建议：通过
理由：[AI 的详细理由]

请审图员根据实际情况进行最终审核。
```

**不自动操作**：由审图员自行决定是否通过

---

## 🔧 技术实现

### 新增状态

```typescript
const [aiReviewing, setAiReviewing] = useState(false);  // AI 审核中
const [boothId, setBoothId] = useState<string | null>(null);  // 展位 ID
```

### 核心函数

```typescript
const handleAIReview = async () => {
  // 1. 确认对话框
  // 2. 调用 AI 初审 API
  // 3. 解析响应
  // 4. 如果建议驳回，自动填写审核意见
  // 5. 显示结果提示
}
```

### API 调用

```typescript
const response = await fetch(
  `${SUPABASE_URL}/functions/v1/ai-pre-review`,
  {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ booth_id: boothId })
  }
);
```

---

## 🎨 UI 设计

### 按钮样式

**正常状态**：
- 背景色：紫色（`bg-purple-600`）
- 文字：白色
- 图标：机器人（`fa-robot`）
- Hover：深紫色（`hover:bg-purple-700`）

**加载状态**：
- 图标：旋转的加载器（`fa-spinner fa-spin`）
- 文字："AI 分析中..."
- 禁用状态（`disabled`）

**禁用条件**：
- AI 正在分析中
- 正在提交审核
- 未获取到 booth_id

---

## 📝 使用流程示例

### 场景 1：AI 发现问题

1. 审图员打开图纸审核弹窗
2. 点击"AI 初审"按钮
3. 确认对话框 → 点击"确定"
4. 等待 AI 分析（显示"AI 分析中..."）
5. AI 返回：
   ```
   建议：驳回
   理由：图纸中工字钢固定仅使用2枚螺栓，不符合规范要求的4枚螺栓标准...
   ```
6. 系统自动：
   - 将"内部结构图"标记为"不通过"
   - 填写审核意见（包含 AI 的理由）
7. 审图员确认或修改意见
8. 点击"提交审核意见"

### 场景 2：AI 认为合格

1. 审图员打开图纸审核弹窗
2. 点击"AI 初审"按钮
3. AI 返回：
   ```
   建议：通过
   理由：展位高度、面积、结构设计均符合规范要求
   ```
4. 审图员根据实际情况决定是否通过
5. 手动点击各图纸的"通过"或"不通过"
6. 提交审核

---

## ⚠️ 注意事项

1. **AI 仅提供建议**
   - AI 的审核结果不会直接提交到数据库
   - 最终决策权在审图员手中
   - 审图员可以修改或忽略 AI 的意见

2. **需要图纸文件**
   - 如果展位未上传图纸，AI 只能基于文字信息分析
   - 会提示"未找到已上传的图纸"

3. **API 调用时间**
   - 多模态视觉模型分析需要时间（约 5-15 秒）
   - 请耐心等待"AI 分析中..."状态

4. **错误处理**
   - 如果 API 调用失败，会显示错误提示
   - 不会影响正常的手动审核流程

---

## 🚀 下一步

现在可以：

1. **重新部署 Edge Function**（如果还没部署）
   ```powershell
   supabase functions deploy ai-pre-review --project-ref aakexkggqspgpimfwlkn
   ```

2. **启动前端测试**
   ```powershell
   npm run dev
   ```

3. **测试完整流程**：
   - 登录审图员账号
   - 打开特装审图页面
   - 点击某个展位的"图纸审核"
   - 点击"AI 初审"按钮
   - 查看 AI 的审核建议

---

## 🎯 预期效果

审图员使用 AI 初审后：
- ✅ 节省审图时间（AI 快速识别明显问题）
- ✅ 提高审核准确性（AI 对照规范检查）
- ✅ 保留人工决策权（AI 仅提供建议）
- ✅ 提升工作效率（自动填写审核意见）

完美的人机协作审图流程！🎉
