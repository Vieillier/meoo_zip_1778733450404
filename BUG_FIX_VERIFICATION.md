# ✅ 修复验证指南

## 🎯 修复内容

### 问题
审图员点击"可再次驳回"后，展商无法操作，没有"开启修改模式"按钮。

### 原因
1. 按钮显示条件只检查 `last_reviewed_at`，但驳回时设置为 `null`
2. 审核意见被清空，展商看不到为什么被驳回

### 解决方案
1. 增强按钮显示条件，检查是否有驳回意见
2. 保留审核意见，不清空

---

## 🚀 快速验证（5 分钟）

### 步骤 1：启动开发服务器
```bash
npm run dev
```

### 步骤 2：审图员操作
```
1. 登录审图员账号
2. 找到已提交图纸的展商
3. 打开图纸审核弹窗
4. 对所有图纸点击"通过"
5. 点击"审核通过"按钮
6. 重新打开图纸审核弹窗
7. 点击"可再次驳回"按钮
8. 确认对话框
```

### 步骤 3：展商操作
```
1. 退出审图员账号
2. 登录展商账号
3. 打开图纸申报页面
```

### 步骤 4：验证修复
```
✅ 检查点 1：显示"等待审核"标签
✅ 检查点 2：显示"开启修改模式"按钮（修复）
✅ 检查点 3：点击"开启修改模式"进入编辑状态
✅ 检查点 4：看到审核意见（修复）
✅ 检查点 5：可以修改图纸
✅ 检查点 6：可以提交整改申报
```

---

## 📋 详细验证

### 验证 1：按钮显示

**预期**：
- 显示"等待审核"标签（黄色）
- 显示"开启修改模式"按钮（蓝色）

**检查**：
```javascript
// 打开浏览器开发者工具 (F12)
// 查看 Elements 标签
// 找到 "开启修改模式" 按钮
// 应该存在且可见
```

### 验证 2：进入修改模式

**步骤**：
1. 点击"开启修改模式"按钮
2. 界面变为可编辑状态

**预期**：
- 显示"整改模式"标签（蓝色）
- 上传区域可用
- 可以删除和上传文件

### 验证 3：查看审核意见

**步骤**：
1. 在修改模式下查看图纸卡片
2. 查看是否显示审核意见

**预期**：
- 显示红色背景的审核意见框
- 内容为审图员的驳回意见

### 验证 4：提交整改申报

**步骤**：
1. 修改图纸（可选）
2. 点击"提交整改申报"按钮

**预期**：
- 显示"整改申报已提交，等待审核"提示
- 界面返回到非编辑状态
- 显示"等待审核"标签
- 显示"第 1 轮整改"

---

## 🔍 数据库验证

### 查询 1：验证驳回后的数据

```sql
SELECT 
  booth_number,
  is_submitted,
  last_reviewed_at,
  effect_drawing_status,
  effect_drawing_comment
FROM drawing_documents
WHERE booth_number = '展位号';
```

**预期结果**：
```
is_submitted = true
last_reviewed_at = null
effect_drawing_status = 'pending'
effect_drawing_comment = '审核意见内容'（不为空）✅
```

### 查询 2：验证整改后的数据

```sql
SELECT 
  booth_number,
  is_submitted,
  last_reviewed_at,
  review_round,
  effect_drawing_status
FROM drawing_documents
WHERE booth_number = '展位号';
```

**预期结果**：
```
is_submitted = true
last_reviewed_at = null
review_round = 1（或更高）
effect_drawing_status = 'pending'
```

---

## 🧪 完整测试场景

### 场景 1：再次驳回后展商可以操作

**前置条件**：
- 展商已提交图纸
- 审图员已审核全部通过

**测试步骤**：
1. 审图员点击"可再次驳回"
2. 展商登录后看到"等待审核"
3. ✅ 看到"开启修改模式"按钮
4. 点击进入修改模式
5. ✅ 看到审核意见
6. 修改图纸
7. 提交整改申报

**预期结果**：
- ✅ 所有步骤都能正常执行
- ✅ 与原有的驳回流程一致

### 场景 2：原有驳回流程仍然正常

**前置条件**：
- 展商已提交图纸
- 审图员审核部分驳回

**测试步骤**：
1. 展商看到"等待审核"
2. 看到"开启修改模式"按钮
3. 进入修改模式
4. 看到审核意见
5. 修改图纸
6. 提交整改申报

**预期结果**：
- ✅ 流程与修复前一致
- ✅ 无任何变化

---

## ✅ 修复完成检查表

- [ ] 代码修改完成
- [ ] 无 TypeScript 错误
- [ ] 无 ESLint 警告
- [ ] 快速验证通过
- [ ] 详细验证通过
- [ ] 数据库验证通过
- [ ] 完整测试通过
- [ ] 原有流程仍正常

---

## 🎉 修复完成

所有验证通过后，修复完成！

**下一步**：
1. 提交代码
2. 部署到测试环境
3. 进行完整测试
4. 部署到生产环境

---

## 📝 修改文件列表

1. `src/components/DrawingSubmission.tsx`
   - 新增 `hasRejectionComments()` 函数
   - 修改按钮显示条件

2. `src/components/DrawingReview.tsx`
   - 修改 `handleRejectAgain()` 函数
   - 保留审核意见

---

## 🔗 相关文档

- `BUG_FIX_REJECT_AGAIN.md` - 修复说明
- `DRAWING_REVIEW_FLOW.md` - 流程说明
- `MANUAL_TESTING_GUIDE.md` - 测试指南
